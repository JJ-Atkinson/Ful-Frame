(ns dev.freeformsoftware.dependency-graphs.algorithms.diff
  "Diffing a normalized DB. Tries to be fast, and grabs an ident if possible. Will not
   go deeper than [entity attribute]. ")

(defn walk-diff-maps [a b])

(defn quick-diff 
  "Expects maps"
  [a b]
  (let [root-diffs ]))
